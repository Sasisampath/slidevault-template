# ── MongoDB ─────────────────────────────────────────────────
# Option 1: Local MongoDB (default)
MONGO_URI=mongodb://localhost:27017/slidevault

# Option 2: MongoDB Atlas (free cloud) — paste YOUR string here:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/slidevault?retryWrites=true&w=majority

# ── Server ───────────────────────────────────────────────────
PORT=3000
